{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Seccion list</h1>

    <table id="example" class="display compact" cellspacing="1" width="100%">
          <thead>
            <tr>
                <th>Id</th>
                <th>Codigo</th>
                <th>Ubicacion</th>
                <th>Cantidadespacio</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('seccion_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.codigo }}</td>
                <td>{{ entity.ubicacion }}</td>
                <td>{{ entity.cantidadEspacio }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('seccion_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('seccion_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('seccion_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
